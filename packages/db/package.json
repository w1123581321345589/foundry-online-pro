{
  "name": "@foundry/db",
  "private": true,
  "dependencies": {
    "prisma": "^5.18.0",
    "@prisma/client": "^5.18.0"
  },
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init"
  }
}